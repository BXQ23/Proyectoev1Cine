# URL base del backend. Aca es donde despues, cuando entre el API Gateway,
# solo cambiamos este valor - nada de codigo. Por eso el frontend nunca
# debe tener "http://localhost:8080" escrito a mano en ningun archivo .jsx.
VITE_API_BASE_URL=http://localhost:8080
